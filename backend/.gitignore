# dependencies
/node_modules

# build output
/dist

# tests / coverage
/coverage
*.tsbuildinfo

# local env (never commit real secrets)
.env
.env.local
.env.*.local
# Phase 18 — isolated admin E2E environment (throwaway secrets, but still
# gitignored on principle; see scratch/e2e-infra/)
.env.e2e

# prisma — generated client lives in node_modules; keep migrations tracked
/prisma/*.db

# logs / editor
*.log
.DS_Store
